language: node_js
node_js:
  - "node"

branches:
  only:
    - master

script:
  - npm install -g gulp

cache:
  directories:
    - node_modules

deploy:
  provider: surge
  project: ./
  domain: dev-valassis-design.surge.sh
